import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // TODO: update with real app widget once theme is configured
    expect(1 + 1, equals(2));
  });
}
